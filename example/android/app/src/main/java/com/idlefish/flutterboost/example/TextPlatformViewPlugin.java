package com.idlefish.flutterboost.example;

// TextPlatformViewPlugin 已迁移到 MyApplication.java 中使用 v2 embedding 方式注册 - lijizhi
// 此文件保留用于向后兼容，但不再使用 v1 embedding API
// 实际的注册在 MyApplication.java 的 setup 回调中完成
public class TextPlatformViewPlugin {
    // 已废弃：使用 v2 embedding 方式在 MyApplication 中注册
    // 此方法不再使用，保留仅为向后兼容
}
